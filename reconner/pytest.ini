[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: tests that require external services (ZAP, security tools)

[coverage:run]
source = reconner
omit =
    reconner/tests/*

[coverage:report]
fail_under = 70
show_missing = true
skip_covered = false
